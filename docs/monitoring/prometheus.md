# Prometheus

Prometheus collects time-series metrics from monitored targets and makes them queryable with PromQL.

## Example queries

```promql
node_memory_MemTotal_bytes
```

```promql
node_memory_MemAvailable_bytes
```
